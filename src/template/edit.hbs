import { useParams } from '@umijs/max'; 
import React from 'react'; 
import BasicForm from './basicForm';

const Edit: React.FC = () => {
  const { id } = useParams(); 
  return <BasicForm title="修改" id={id} />
};

export default Edit;